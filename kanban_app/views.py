"""HTTP views for the kanban_app.

This module contains Django view handlers for the kanban application.
Placeholder views can be expanded to render templates or delegate to
APIView endpoints defined in the `kanban_app.api` package.
"""

from django.shortcuts import render

# Create your views here.
