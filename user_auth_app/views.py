"""HTTP views for user authentication app.

This module can host template-backed views for registration, login and
profile pages. Most authentication endpoints are implemented in
`user_auth_app.api` as REST endpoints.
"""

from django.shortcuts import render

# Create your views here.
